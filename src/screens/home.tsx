import {Text} from 'react-native';
import React from 'react';
import {FontSize, Wrapper} from 'rn-theme-park';

const HomeScreen = () => {
  return (
    <Wrapper>
      <Text style={{fontSize: FontSize.lg}}>HomeScreen</Text>
    </Wrapper>
  );
};

export default HomeScreen;
